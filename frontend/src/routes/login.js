import React from 'react'
import LoginForm from '../components/LoginForm'

const Login = () => {
    return (
        <div style={{height: '100vh'}} className="login d-flex justify-content-center align-items-center">
            <LoginForm />
        </div>
    )
}

export default Login